---
title: "Wan-Streamer v0.1: End-to-end Real-time Interactive Foundation Models"
title_zh: Wan-Streamer v0.1：端到端实时交互基础模型
authors: "Lianghua Huang, Zhifan Wu, Wei Wang, Yupeng Shi, Mengyang Feng, Junjie He, Chenwei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chen Liang, Cheng Yu, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Zoubin Bi"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25041v1"
tags: ["query:wan-av-gen"]
score: 10.0
evidence: 使用WAN架构的端到端音视频交互模型
tldr: Wan-Streamer是一个原生流式端到端交互基础模型，用于实时全双工音视频交互。它采用单一Transformer同时建模语言、音频和视频的输入和输出，通过交错序列和块因果注意力实现增量流式处理。该方法摒弃了传统的级联式独立模块，直接实现感知、推理与生成。实验表明该模型能够实时生成高质量的音视频内容，为多模态交互应用提供新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25041v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1700, \"height\": 799, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25041v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 458, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25041v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1725, \"height\": 897, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25041v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1728, \"height\": 1201, \"label\": \"Table\"}]"
motivation: 使用WAN架构的端到端音视频交互模型。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
我们提出了Wan-Streamer，一个原生流式、端到端的交互式基础模型，从零开始设计用于实时、低延迟、全双工的视听交互。Wan-Streamer在单个Transformer中无缝地将语言、音频和视频同时作为输入和输出进行建模，其中序列被表示为交错的视觉、音频和文本输入标记以及视觉、音频和文本输出标记，并通过块因果注意力机制实现增量流式处理。与依赖独立VAD、ASR、语言、TTS、音频驱动动画或视频生成模块的级联交互系统不同，Wan-Streamer不依赖于外部语言、语音、化身或视频生成模块：感知、推理、生成、响应时机、对话轮次管理以及跨模态同步在单一统一模型内联合学习，减少了管道延迟和错误累积。为了支持自然的视听响应能力，我们围绕可流式处理重新设计了整个技术栈，包括因果编码器、因果解码器、块因果注意力以及低延迟多模态标记调度，使得流式单元短至160毫秒（25 fps）。Wan-Streamer实现了约200毫秒的模型端响应延迟，结合350毫秒的双向网络延迟后，总交互延迟约为550毫秒，支持亚秒级的双工视听通信。这些结果使Wan-Streamer成为一个统一、端到端的多模态交互基础模型，适用于低延迟流式交互。

## Abstract
We present Wan-Streamer, a native-streaming, end-to-end interactive foundation model designed from the ground up for real-time, low-latency, full-duplex audio-visual interaction. Wan-Streamer seamlessly models language, audio, and video as both input and output within a single Transformer, where the sequence is represented as interleaved visual, audio, and text input tokens together with visual, audio, and text output tokens, coordinated by block-causal attention for incremental streaming. Unlike cascaded interactive systems that rely on separate VAD, ASR, language, TTS, audio-driven animation, or video-generation modules, Wan-Streamer does not rely on external language, speech, avatar, or video-generation modules: perception, reasoning, generation, response timing, turn management, and cross-modal synchronization are learned jointly within one unified model, reducing pipeline latency and error accumulation. To support natural audio-visual responsiveness, we redesign the entire stack around streamability, including causal encoders, causal decoders, block-causal attention, and low-latency multimodal token scheduling, enabling streaming units as short as 160 ms at 25 fps. Wan-Streamer achieves approximately 200 ms model-side response latency and approximately 550 ms total interaction latency when combined with 350 ms bidirectional network latency, supporting sub-second duplex audio-visual communication. These results position Wan-Streamer as a unified, end-to-end, multimodal interactive foundation model for low-latency streaming interaction.

---

## 论文详细总结（自动生成）

好的，遵照您的要求，以下是对提供的论文《Wan-Streamer v0.1: End-to-end Real-time Interactive Foundation Models》的结构化综述。

### 1. 论文的核心问题与整体含义

*   **研究动机**：当前的人工智能交互系统大多采用级联（cascaded）架构，即由独立的语音活动检测(VAD)、自动语音识别(ASR)、大语言模型(LLM)、文本转语音(TTS)和动画/视频生成模块串联而成。这种管线式设计带来了模块边界的等待时间、错误累积，且难以学习自然的响应时机、对话轮次管理和视听同步等行为。
*   **核心问题**：如何构建一个能从零开始进行实时、低延迟、全双工（能同时听和说）视听交互的原生流式模型。
*   **整体含义**：论文旨在证明，通过将感知、推理、语音生成、视觉生成和交互管理整合进单一端到端模型，可以克服传统级联系统的根本局限，实现更接近人类的自然、同步、低延迟交互。这为具身助理、实时数字人等领域提供了统一的基础模型范式。

### 2. 论文提出的方法论

论文提出了一套以“流式能力是建模约束”为核心理念的端到端架构，其主要技术细节如下：

*   **统一序列建模**：
    *   将用户和智能体的语言、音频、视频同时作为输入和输出，表示为一条交错的因果序列，由**单一Transformer**处理。
    *   采用**块因果注意力**机制实现增量流式生成，使每个新生成单元能立即被提交到交互历史中作为后续上下文。

*   **全因果技术栈**：
    *   **音频和视频变分自编码器**：设计为严格的因果结构，支持流式潜空间编码。
    *   **模型组件**：采用因果视听编码器、因果音频与视频解码器，以及一个时间上因果的Transformer。

*   **联合训练目标**：
    *   **文本响应**：表示为离散token，通过下一token预测和交叉熵损失优化。
    *   **音频和视频响应**：在连续潜空间中表示，通过**条件流匹配**联合生成。模型学习估计含噪视听潜变量(\(z^a_\tau， z^v_\tau\))的速度场，条件为干净的流式上下文(\(c_k\))和噪声水平(\(\tau\))。目标函数为：
        \[
        \mathcal{L}^m_{FM} = \mathbb{E}_{\epsilon^m} \left\| f_\theta(z^a_\tau， z^v_\tau， c_k， \tau) - \frac{\partial z^m_\tau}{\partial \tau} \right\|_2^2
        \]
        其中，\(f_\theta\)表示统一的扩散Transformer，\(m \in \{a， v\}\)。

*   **低延迟推理系统（Thinker-Performer架构）**：
    *   将模型部署为一个分离的流水线，以最大化计算重叠和硬件利用率。
    *   **Thinker（思考者）**：负责当前用户单元感知、语言状态更新、KV缓存构建，以及前一响应单元的音频视频解码与输出。
    *   **Performer（执行者）**：仅承载下一单元视听潜变量的流匹配去噪生成过程。
    *   两者通过交换KV缓存和潜变量来维持统一的模型状态，实现感知、状态更新、前一帧解码、下一帧生成和通信的流水线重叠。

### 3. 实验设计

*   **训练数据**：
    *   使用理解型数据（图像/音频/视频理解、文本对话等）、生成型数据（图/音/视频生成）和**端到端双工交互数据**的混合数据集。
    *   训练分为三阶段：独立任务预训练、端到端交互训练、以及面向低延迟流式的**蒸馏**。

*   **Benchmark与对比方法**：
    *   主要衡量指标是**响应延迟**。对比分为两类：
        1.  **语音与全模态系统（Table 1）**：对比了Doubao Realtime Voice， GPT-4o Realtime API， Moshi， Qwen3-Omni等。论文特别强调统一端到端模型的延迟覆盖了文字、语音和视觉的完整响应路径，而其他系统多只报告部分模态或部分管线的延迟。
        2.  **视觉交互系统与组件（Table 2）**：对比了X-Streamer, VASA-1, TalkingMachines等。论文区分了“完整交互环路系统”和“渲染/生成组件”，指出后者虽快但不包含对话感知等完整路径。

### 4. 资源与算力

*   文中**未明确提及**训练模型所用的GPU型号、总数量或训练总时长。
*   推理部分提到使用双GPU部署Thinker-Performer架构，以达到实时吞吐和延迟要求，但同样未指定GPU型号。

### 5. 实验数量与充分性

*   **实验数量**：论文本身不包含传统意义上的多种消融实验或跨数据集测评。核心实验是围绕**延迟和运行时表现**的对比（Tables 1 & 2）以及定性的交互自然度描述（如闲时行为、打断、主动发言）。
*   **充分性与公平性**：作者在对比延迟时，非常注重测量边界，将“模型端延迟”、 “包含网络的总延迟”与“首包延迟”、 “组件延迟”区分开，这提升了对比的客观性。然而，由于缺乏自动化指标（如生成质量FID、语音识别错误率WER、用户偏好研究等）和标准Benchmark，当前实验更偏向于**系统能力的概念验证**，证明了模型架构的可行性和流式特性，但对于模型熟读、生成质量等多维度效果的充分性和统计显著性验证尚有不足。

### 6. 论文的主要结论与发现

*   **可行性**：证明了构建一个在单一Transformer中感知并生成语言、语音和同步视觉行为的端到端、全双工交互模型是完全可行的。
*   **延迟表现**：该模型在双GPU推理下达到~200ms模型端响应延迟，加上350ms网络延迟后总延迟~550ms，实现了**亚秒级**全双工交互。
*   **交互自然性**：由于统一建模，模型展现了更自然的交互行为，如在空闲和聆听时生成持续的视觉状态（如微表情、姿态），以及基于视觉线索的主动发言能力，而不是被动问答循环。

### 7. 优点

*   **架构创新性强**：完全摒弃级联系统，首次在单一Transformer内实现原生的、全模态因果流式交互，将“可流式处理”提升为核心设计哲学。
*   **推理系统设计精巧**：提出的Thinker-Performer分离架构，通过KV缓存交换实现感知与生成的重叠，巧妙地解决了统一模型内部不同任务计算量不均和延迟耦合的问题，在不改变模型语义的前提下实现了高吞吐和低延迟。
*   **严谨的延迟评估**：对比实验时，对“延迟”的测量边界有清晰定义和区分，避免了与业界不完整延迟数字的直接比较，体现了学术严谨性。

### 8. 不足与局限

*   **生成质量与分辨率有限**：论文明确说明当前版本(v0.1)的视觉输出分辨率为192p，仅作为概念验证，生成质量尚未达到实用级别，高分辨率版本留给未来工作。
*   **评估指标单一**：缺乏对生成内容质量（如图像保真度、唇音同步精度、语音自然度）、交互成功率和用户主观体验的量化评估，无法衡量模型的综合能力。
*   **算力开销未知**：未披露训练所需的具体算力资源，使得外部人员难以评估其训练成本和可复现性。
*   **对比实验深度不足**：主要停留在延迟对比上，缺少在标准对话、语音合成或视频生成基准上与现有成熟方法的性能比较。
*   **应用场景验证缺失**：所有交互自然度的描述均为定性展示，没有在具体的应用任务（如数字人客服、直播）中提供定量或比较性的用户研究。

（完）
