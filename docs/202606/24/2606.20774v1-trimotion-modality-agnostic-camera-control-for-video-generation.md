---
title: "TriMotion: Modality-Agnostic Camera Control for Video Generation"
title_zh: TriMotion：面向视频生成的模态无关相机控制
authors: "Seunghyun Shin, Jifei Song, Wooseok Jeon, Hae-Gon Jeon, Jiankang Deng"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20774v1"
tags: ["query:wan-av-gen"]
score: 8.0
evidence: 模态无关的相机控制视频生成框架
tldr: 视频生成中的相机运动控制至关重要，但现有方法仅支持单一控制模态，难以融合视频、位姿和文本描述。TriMotion提出模态无关框架，通过共享运动嵌入空间统一映射异构输入，并基于几何外参构建Motion Triplet数据集，及潜在运动一致性损失，驱动生成视频在潜在空间精确跟随目标轨迹。实验显示，该方法能生成高质量视频，准确跟随三种输入模态下的相机轨迹，并避免了像素空间解码开销。此外，共享嵌入空间支持顺序运动合成与跨模态运动插值，扩展了视频生成的应用场景。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20774v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1215, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20774v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1255, \"height\": 670, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20774v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1249, \"height\": 724, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20774v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1250, \"height\": 854, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20774v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1246, \"height\": 863, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20774v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1267, \"height\": 500, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20774v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1264, \"height\": 233, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20774v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1267, \"height\": 175, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20774v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1264, \"height\": 393, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20774v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1096, \"height\": 342, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20774v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1119, \"height\": 342, \"label\": \"Table\"}]"
motivation: 现有相机运动控制方法仅支持单一模态，无法融合视频、位姿和文本等异构输入，限制用户灵活交互。
method: TriMotion 将多种输入映射到共享运动嵌入空间，利用基于几何外参的 Motion Triplet 数据集和潜在运动一致性损失进行训练。
result: 在三种输入模态下，TriMotion 均能生成高质量且精确跟随目标相机轨迹的视频，并避免像素空间解码开销。
conclusion: TriMotion 实现了模态无关的相机控制视频生成，共享嵌入空间还支持运动组合与跨模态插值等灵活应用。
---

## 摘要
相机运动控制对于在生成系统中引导视点变化至关重要。然而，现有方法通常将生成过程依赖于单一特定模态，如显式姿态轨迹或参考视频，这限制了它们支持异构用户输入的能力。为了解决这一局限，我们提出了 TriMotion，一个模态无关的相机控制视频生成框架，它将描述相同相机轨迹的视频、姿态和文本输入映射到共享的运动嵌入空间。学习这样的空间需要跨模态的同步监督。因此，我们通过扩展多相机视频数据集，利用从相机外参导出的基于几何的运动描述，构建了运动三元组数据集。我们还引入了一个潜在运动一致性目标，利用运动嵌入空间鼓励生成的视频直接在潜在空间中遵循目标相机轨迹，从而避免像素空间解码的成本。大量实验表明，TriMotion 能够生成高质量的视频，并在所有三种模态下准确遵循目标相机轨迹。除了标准的生成任务，共享的运动嵌入空间还支持灵活的应用，如顺序运动组合和跨模态运动插值。

## Abstract
Camera motion control is essential for directing viewpoint changes in generative systems. However, existing methods typically condition the generation process on a single specific modality, such as explicit pose trajectories or reference videos, limiting their ability to support heterogeneous user inputs. To address this limitation, we present TriMotion, a modality-agnostic framework for camera-controlled video generation that maps video, pose, and text inputs, describing the same camera trajectory into a shared motion embedding space. Learning such a space requires synchronized supervision across modalities. Therefore, we build the Motion Triplet Dataset by extending a Multi-Cam Video Dataset with geometry-grounded motion descriptions derived from camera extrinsics. We further introduce a latent motion consistency objective that leverages the motion embedding space to encourage the generated video to follow the target camera trajectory directly in latent space, avoiding the cost of pixel-space decoding. Extensive experiments show that TriMotion generates high-quality videos that accurately follow the target camera trajectories across all three modalities. Beyond standard generation, the shared motion embedding space also enables flexible applications such as sequential motion composition and cross-modal motion interpolation.

---

## 论文详细总结（自动生成）

好的，请稍等。根据您提供的论文内容，我将按照要求生成一份结构化的详细中文总结。

---

### 1. 论文的核心问题与整体含义

*   **研究问题**：视频生成中的相机运动控制对于引导观众视点、构建电影感至关重要。现有方法通常局限于单一的控制模态（如相机姿态轨迹、参考视频或文本描述），无法融合不同形式的用户输入，限制了生成系统的灵活性、易用性和可控精度。
*   **整体含义**：本文提出了一个名为 **TriMotion** 的模态无关框架，旨在突破单一模态的限制。其核心思想是学习一个统一的**共享运动嵌入空间**，将描述同一相机轨迹的视频、位姿、文本三类异构输入映射到该空间中，从而实现跨模态的一致相机控制，并支持如运动组合与插值等新应用。

### 2. 论文提出的方法论

*   **核心思想**：构建一个能够理解来自不同模态的相机运动意图的统一表示空间，并利用该表示驱动视频生成模型，使之能精确、一致地执行相机运动。
*   **关键技术细节**：
    *   **运动三元组数据集 (Motion Triplet Dataset)**：
        *   基于现有的多相机视频数据集（Multi-Cam Video Dataset，包含精确的相机外参），利用大语言模型将原始相机轨迹转化为基于几何的文本运动描述，从而为每个样本创建了（视频，位姿，文本）的对齐三元组。
        *   **轨迹-文本转换流程**：先将位姿数据转换为相对轨迹，并提取为规范化的符号序列（如“Dolly In + Pan Right”)，再引导LLM根据此序列生成自然语言描述。
    *   **统一运动嵌入空间 (Unified Motion Embedding Space)**：
        *   为视频（基于VGGT）、文本（基于冻结的T5编码器+可学习的时序查询）、位姿（帧级MLP）三种模态分别设计编码器，将输入映射为固定长度的运动令牌序列。
        *   通过一个轻量级的时序Transformer对这些令牌进行建模，形成最终的嵌入序列。
        *   训练该空间的复合损失函数（公式9）包括：
            *   全局对齐（`LNCE`）：基于InfoNCE损失的对比学习，拉近不同模态的全局运动令牌。
            *   时间同步（`Ltemp`）：最小化不同模态对应时间令牌的余弦距离。
            *   几何保真正则化（`Lpose`）：强制各模态嵌入都能回归出相机外参，以保留物理上有意义的3D几何信息。
    *   **潜在运动一致性 (Latent Motion Consistency)**：
        *   为了避免在像素空间解码的巨大开销，作者引入了一个**运动嵌入预测器 (`Mpred`)**。
        *   `Mpred` 经过预训练，能从VAE编码的视频潜在表示 `z0` 中预测其运动嵌入序列。
        *   在扩散模型训练时，冻结的 `Mpred` 接收模型基于当前噪声预测重建的干净潜在 `ẑ0`（公式11），并计算其与目标运动嵌入 `em` 的余弦相似度损失（`Lmotion`，公式10），从而直接在潜在层面约束生成视频的相机运动。
    *   **生成流程与训练**：
        *   基于潜在视频扩散Transformer，将源视频和目标噪声潜在沿时间维拼接作为输入。
        *   将统一运动空间输出的运动嵌入序列通过残差连接方式注入到扩散主干的每个Transformer块中（公式3），以控制生成过程。
        *   总损失函数（公式12）为去噪损失（`Ldenoise`）和潜在运动一致性损失（`Lmotion`）的加权和。训练时仅微调扩散模型的时空注意力层和投影MLP，保留生成先验。

### 3. 实验设计

*   **数据集与场景**：
    *   **训练集**：基于自建的**运动三元组数据集**（约136K视频），包含真实的渲染场景和精确的相机外参。
    *   **评估集**：使用来自RealEstate10K的500个视频-位姿对，以及来自Koala-36M的500个真实世界视频，覆盖了图像到视频（I2V）和视频到视频（V2V）两种核心任务场景。
*   **基准方法与对比对象**：
    *   **I2V任务**：与CamI2V（位姿条件）、MotionClone（视频条件）和CamCloneMaster（视频条件）等最先进的方法进行对比。
    *   **V2V任务**：与DaS（位姿条件）、TrajectoryCrafter（位姿条件）、ReCamMaster（位姿条件）和CamCloneMaster（视频条件）对比。
    *   自身模态内也进行了 TriMotion (Text) 、 TriMotion (Video) 、 TriMotion (Pose) 的对比。
*   **评估指标**：全面评估了生成视频的三个方面：
    *   **视觉质量**：FVD, FID, CLIP Score, VBench图像质量/美学质量。
    *   **动态质量**：CLIP-F, VBench运动平滑度、闪烁、主体/背景一致性。
    *   **相机运动精度**：通过MegaSaM估计生成视频的相机位姿，计算旋转误差（`Erot`）、平移误差（`Etrans`）和相机运动一致性（`CamMC`）。
    *   **V2V特有**：FVD-V和CLIP-V用于衡量源内容保持度。

### 4. 资源与算力

*   论文明确提到了训练算力资源：所有模型均在 **4 块 NVIDIA H200 GPU** 上进行训练。
*   使用 **AdamW** 优化器（学习率 `1e-4`, `bf16` 混合精度），并采用梯度裁剪。
*   **训练时长/迭代数**：
    *   统一运动空间：100个epoch。
    *   运动嵌入预测器：10个epoch。
    *   扩散模型骨干：10K次迭代。
*   训练框架和条件设置详细，具备良好的可复现性。

### 5. 实验数量与充分性

*   **实验数量较多，设计充分**：
    *   **主实验**：在I2V和V2V两个任务上，与6种以上的SOTA方法进行了详尽的定量对比，覆盖三大类共十几种指标。
    *   **消融实验**：针对核心设计（仅用目标运动注入 vs. 拼接/减法）和关键组件（`Lmotion` 损失）进行了消融研究，验证了其有效性。
    *   **分析实验**：对统一运动空间的跨模态对齐能力和几何保真度进行了专门分析，证明了空间的优越性。
    *   **应用展示**：展示了顺序运动合成和跨模态运动插值两种新应用。
    *   **用户调研**：通过MTurk平台对描述质量、组合效果和生成质量进行了主观评价，增强了说服力。
*   **客观性与公平性**：所有对比方法均使用官方实现和预训练模型进行评估，使用统一的标准测试集和度量指标，保证了比较的公平性和客观性。

### 6. 论文的主要结论与发现

*   TriMotion在I2V和V2V任务上均实现了**视觉质量、动态稳定性和相机运动精度的最佳平衡**。
*   其位姿条件变体在大部分指标上表现最佳，而视频和文本条件变体同样具有竞争力，证明**共享运动空间对所有三种模态均提供了有效的控制信号**。
*   **潜在运动一致性损失是提升轨迹保真度的关键**，单纯依靠运动注入条件生成是不够的。
*   统一运动嵌入空间不仅能精确地区分不同运动模式（而非场景身份），还能**保留有意义的几何信息**，甚至从文本嵌入中也能回归出相机位姿。
*   该框架成功实现了**顺序运动组合和跨模态运动插值**等灵活应用，无需重新训练或人工设计轨迹。

### 7. 优点
*   **模态无关的创新性**：首次将视频、位姿和文本三种相机控制模态统一到一个框架中，极大地提升了系统的灵活性和用户友好度。
*   **方法设计的系统性**：从**数据集构建**（基于几何导引的文本描述）到**表示学习**（统一空间与多目标对齐），再到**生成控制与约束**（潜在一致性损失），整个流程自成体系且设计严谨。
*   **效率与精度的平衡**：通过在潜在空间执行运动一致性约束，**避免了昂贵的像素空间解码**，实现了高效且精确的轨迹控制。
*   **应用可扩展性强**：共享的嵌入空间天然支持运动组合和插值，展示了强大的应用潜力，超越了简单的单模态生成。
*   **评估全面**：实验评估涵盖定量、定性、消融、分析和用户研究多个维度，论证有力。

### 8. 不足与局限
*   **极端轨迹的外推伪影**：论文作者明确指出，在处理极端相机运动（需要合成大面积未见区域）时，仍然可能出现边界外推伪影，这是当前方法的一个共同挑战。
*   **数据与场景偏向**：训练数据基于Unreal Engine 5渲染的合成场景，虽然规模大且真值精确，但其在极度复杂、光照多变或物理属性独特的真实世界视频上的泛化能力可能需要进一步验证。
*   **评估集来源**：评估集使用了RealEstate10K（以房地产室内外场景为主），场景多样性有限，可能无法完全代表所有开域视频的复杂性。
*   **运动描述的复杂度**：生成的文本描述被限于“避免公制数值”的定性风格，虽然贴近用户输入，但可能难以表达需要精确量化控制的运动，如“每秒移动0.5米”。对于非常规或高度非线性的复杂混合运动的描述能力也未深入探讨。
*   **技术耦合性**：方法基于现有的视频生成主干（如Wan）和VGGT等预训练模型，其性能上限部分受限于这些基础模型的发展。

（完）
