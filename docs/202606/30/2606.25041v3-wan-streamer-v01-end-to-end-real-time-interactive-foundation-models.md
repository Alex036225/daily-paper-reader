---
title: "Wan-Streamer v0.1: End-to-end Real-time Interactive Foundation Models"
title_zh: Wan-Streamer v0.1：端到端实时交互基础模型
authors: "Lianghua Huang, Zhi-Fan Wu, Wei Wang, Yupeng Shi, Mengyang Feng, Junjie He, Chen-Wei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chen Liang, Cheng Yu, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Yuzheng Wang, Zoubin Bi"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25041v3"
tags: ["query:wan-av-gen"]
score: 9.0
evidence: Wan-Streamer是一个端到端多模态基础模型，用于音视频交互，直接对应WAN多模态生成架构
tldr: 实时音视频交互系统多采用级联模块，导致延迟高、错误累积。Wan-Streamer提出端到端Transformer模型，以块因果注意力统一处理交错视觉、音频、文本流式token，从感知到生成全链路联合学习。模型响应延迟约200ms，总交互延迟约550ms，实现亚秒级全双工通信。该工作为低延迟流式多模态交互树立了统一基础模型范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25041v3/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1700, \"height\": 799, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25041v3/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 458, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25041v3/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1725, \"height\": 897, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25041v3/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1728, \"height\": 1201, \"label\": \"Table\"}]"
motivation: 传统级联系统依赖分离的ASR、TTS等模块，推理延迟高且跨模态同步差，难以实现实时全双工交互。
method: 在单个Transformer中采用块因果注意力，将视觉、音频、文本token交织为流式序列，并设计低延迟token调度，实现160ms粒度的增量生成。
result: 模型侧响应延迟约200ms，结合350ms双向网络延迟后总交互约550ms，支持25fps下亚秒级全双工音视频通信。
conclusion: Wan-Streamer证明了端到端联合建模可消除级联延迟与错误，为实时流式多模态交互提供了高效的统一框架。
---

## 摘要
我们提出了Wan-Streamer，一个原生流式、端到端的交互式基础模型，专为实时、低延迟、全双工视听交互而设计。Wan-Streamer在单个Transformer内无缝地将语言、音频和视频作为输入和输出进行建模，其中序列表示为交错的视觉、音频和文本输入令牌以及视觉、音频和文本输出令牌，并通过块因果注意力协调以实现增量流式传输。与依赖于独立的VAD、ASR、语言、TTS、音频驱动动画或视频生成模块的级联交互系统不同，Wan-Streamer不依赖于外部的语言、语音、虚拟形象或视频生成模块：感知、推理、生成、响应时机、轮次管理和跨模态同步在一个统一模型中联合学习，从而减少了管道延迟和错误累积。为了支持自然的视听响应能力，我们围绕可流式性重新设计了整个堆栈，包括因果编码器、因果解码器、块因果注意力和低延迟多模态令牌调度，使得流式单元可以短至160毫秒，帧率为25 fps。Wan-Streamer在模型侧的响应延迟约为200毫秒，当结合350毫秒的双向网络延迟时，总交互延迟约为550毫秒，支持亚秒级双工视听通信。这些结果使Wan-Streamer成为用于低延迟流式交互的统一、端到端、多模态交互式基础模型。

## Abstract
We present Wan-Streamer, a native-streaming, end-to-end interactive foundation model designed from the ground up for real-time, low-latency, full-duplex audio-visual interaction. Wan-Streamer seamlessly models language, audio, and video as both input and output within a single Transformer, where the sequence is represented as interleaved visual, audio, and text input tokens together with visual, audio, and text output tokens, coordinated by block-causal attention for incremental streaming. Unlike cascaded interactive systems that rely on separate VAD, ASR, language, TTS, audio-driven animation, or video-generation modules, Wan-Streamer does not rely on external language, speech, avatar, or video-generation modules: perception, reasoning, generation, response timing, turn management, and cross-modal synchronization are learned jointly within one unified model, reducing pipeline latency and error accumulation. To support natural audio-visual responsiveness, we redesign the entire stack around streamability, including causal encoders, causal decoders, block-causal attention, and low-latency multimodal token scheduling, enabling streaming units as short as 160 ms at 25 fps. Wan-Streamer achieves approximately 200 ms model-side response latency and approximately 550 ms total interaction latency when combined with 350 ms bidirectional network latency, supporting sub-second duplex audio-visual communication. These results position Wan-Streamer as a unified, end-to-end, multimodal interactive foundation model for low-latency streaming interaction.

---

## 论文详细总结（自动生成）

好的，以下是基于所提供论文的详细中文总结。

### 1. 论文的核心问题与整体含义

本研究旨在解决当前实时多模态交互系统面临的核心挑战：**级联架构带来的高延迟与错误累积**。

-   **研究动机**：人类交互是流式（streaming）、全双工（full-duplex）的，即感知、思考与表达过程是重叠并行的。然而，现有的人工智能交互系统多采用级联（cascaded）设计，将语音活动检测（VAD）、自动语音识别（ASR）、大语言模型（LLM）、文本转语音（TTS）、动画生成等模块串联起来。这种“文本作为中间表示”的管道式架构，不仅在各模块边界引入了等待时间，还会积累识别与同步误差，难以实现自然的响应时机、轮次管理和身份一致性。
-   **整体含义**：论文提出了**Wan-Streamer**，一个从底层设计的原生流式、端到端多模态交互基础模型。其核心意义在于**挣脱了模块化组装的设计范式**，证明了感知、推理、生成、响应时机等所有环节可以在单一Transformer模型中联合学习与推理，从而实现亚秒级的低延迟全双工视听通信。

### 2. 论文提出的方法论

Wan-Streamer 的核心方法论是**围绕“可流式性”（Streamability）构建一个完全因果（Causal）的统一Transformer架构**，将交互视为一个连续、交错的因果序列。

-   **核心思想与建模**：
    -   **统一序列表示**：模型在一个Transformer中处理语言、音频、视频，将它们视为交错的输入和输出令牌（token）。在第k个流式单元，用户输入`u_k`（文本、音频、视频）和代理响应`y_k`（文本、音频、视频）共同构成一个连续流。
    -   **全因果架构**：整个堆栈被重新设计为严格因果的，包括：
        -   **因果VAEs**：用于音频和视频的流式潜在编码。
        -   **因果编码器/解码器**：确保每个新观察到的单元能立即被使用，生成的单元能立即被发出并提交回交互历史。
        -   **块因果注意力**：在Transformer中协调上下文，实现增量流式生成。
    -   **低延迟调度**：多模态令牌的调度机制使得流式单元可以短至**160毫秒**，视频帧率为**25 fps**。

-   **关键训练流程**：
    1.  **预训练阶段**：在一个混合了理解、生成和交互数据的大型数据集上，初始化并训练统一的Transformer，使感知、语言推理和潜在生成能力对齐。
    2.  **端到端交互训练**：在双工交互数据上微调，模型学习根据当前用户输入更新状态、生成同步的多模态响应，并将生成的干净潜在变量（latents）提交回历史上下文，以学习响应时机、聆听行为和打断处理。
    3.  **低延迟流式蒸馏**：将一个使用无分类器引导（CFG）的强教师模型，蒸馏为一个高效学生模型，以在部署时减少流匹配（Flow-Matching）解算器步数，同时通过自强制（self-forcing）策略与分布匹配蒸馏来缓解长序列生成的质量退化。

-   **推理部署策略（Thinker-Performer）**：
    -   模型虽为端到端训练，但部署时被分为**思考者（Thinker）** 和**表演者（Performer）** 两部分，以流水线方式重叠计算，最大化硬件利用率。
    -   **思考者**负责：编码当前用户视听观测、运行Transformer更新状态和KV缓存、将前一步骤生成的音视频潜在变量解码为最终输出。
    -   **表演者**负责：从思考者接收最新的KV缓存，并仅运行流匹配解算器以生成当前步骤的干净视听潜在变量。
    -   这种设计使得**当前帧的感知/状态更新、前一帧的输出解码、下一帧的潜在去噪和KV/潜在变量通信**可以在相邻流式单元间重叠执行。

### 3. 实验设计

论文主要通过**延迟（Latency）和系统覆盖范围**的对比来验证其方法的有效性。

-   **Benchmark与对比场景**：文中并未采用传统的静态数据集进行精度评分，而是将**响应延迟**和**交互范围**作为核心基准，与当前最先进的实时对话和虚拟人系统进行对比。
    -   **Table 1** 对比了与语音和全模态对话系统的用户可感知响应延迟，包括 Doubao Realtime Voice， GPT-4o， Moshi， Qwen-Omni 等。
    -   **Table 2** 对比了与视觉代理、流式虚拟人、音视频生成系统的运行时指标（如FPS）和它们所覆盖的功能范围，包括 MIDAS， U-Mind， VASA-1， StreamAvatar 等。

-   **评估指标**：
    -   **模型侧延迟**：从接收到160ms用户流式单元到输出对应音视频响应单元的时间，Wan-Streamer约为**200 ms**。
    -   **总交互延迟**：加上350ms的双向网络预算后，总延迟约为**550 ms**。
    -   **功能覆盖范围**：是否有同步视觉生成，是否依赖外部模块。论文强调其模型是唯一一个在单一因果流中同时覆盖文本I/O、语音、同步视觉响应的端到端系统。

### 4. 资源与算力

-   **推理算力**：在推理部署上，论文明确提到Thinker-Performer架构是基于**双GPU（two-GPU）** 服务路径实现的，以实现计算重叠和最大化吞吐量。
-   **训练算力**：文中未明确提及训练阶段所使用的GPU型号、总数量或训练总时长。仅描述了其三阶段训练流程（预训练、交互训练、蒸馏）。

### 5. 实验数量与充分性

-   **实验充分性**：作为一篇系统类论文（v0.1版本），其实验焦点并非追求在多个学术基准上的最高分，而是**验证其架构设计在低延迟和统一交互上的可行性**。
-   **对比的客观公平性**：论文非常明确地指出了其延迟指标与其他系统报告的指标的**测量边界差异**（如模型内部延迟 vs. 用户可感知延迟），这增强了对比的客观性和说服力。它通过功能覆盖范围的对比（Table 2）清晰展示了其在“端到端”整合方面的优势，而不仅仅是速度对比。
-   **局限性**：报告主要是概念验证性质的，输出分辨率仅验证了**192p**的初步结果。此外，像“自然度”、“打断”和“主动说话”等交互质量的提升多为定性描述，缺乏系统性的定量用户研究或客观指标（如词错率、FID等）支持。

### 6. 论文的主要结论与发现

-   **Wan-Streamer成功构建了一个原生全双工的视听交互模型**，将感知、推理、语音和视觉生成统一于一个因果流中，跳出了级联系统的框架。
-   该架构通过全因果设计，达成了**模型侧约200ms**和**总交互约550ms**的亚秒级响应延迟，证明了其低延迟流式交互的能力。
-   论文的结论是，未来的实时多模态智能体应当被设计为**原生全双工系统**，其“听、看、说和可见响应”应在一个模型内联合学习，而不是像后处理模块一样被组装在一起。

### 7. 优点

-   **架构统一性**：最大亮点是真正的端到端设计。在一个Transformer中联合建模所有模态和I/O，杜绝了模块间的误差累积和延迟。
-   **全因果低延迟设计**：从VAE到注意力机制的全链路因果改造，以及Thinker-Performer的流水线推理，为实现原生流式交互提供了坚实的技术保证。
-   **交互连贯性**：通过联合学习，模型能够生成同步的音视频响应，自然地处理聆听状态、打断和主动发言，提升了交互的真实感。
-   **对比的透明度**：在和其他系统对比时，清晰界定了“模型侧”、“总延迟”及“功能范围”等测量边界，避免了不公平比较。

### 8. 不足与局限

-   **生成质量与分辨率**：作为v0.1版本，其输出分辨率仅为**192p**，是一个概念验证。尚未在高分辨率下的视频和音频质量上进行验证。
-   **评估不全面**：缺乏对生成内容质量的定量评估（如视频真实度、语音自然度、语义准确性），其“自然性”等说法主要为定性描述。
-   **对比基准不统一**：由于系统设计理念超前，其他比较系统多为级联络线，难以在完全一致的基准上进行公平的端到端性能对比。
-   **训练细节与算力缺失**：未提供训练算力、数据集具体规模和构成等关键细节，影响了其方法可复现性的评估。

（完）
