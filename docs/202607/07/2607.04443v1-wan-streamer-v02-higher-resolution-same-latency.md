---
title: "Wan-Streamer v0.2: Higher Resolution, Same Latency"
title_zh: Wan-Streamer v0.2：更高分辨率，相同延迟
authors: "Lianghua Huang, Zhi-Fan Wu, Yupeng Shi, Wei Wang, Mengyang Feng, Junjie He, Chen-Wei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chen Liang, Cheng Yu, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Yuxiang Bao, Yuzheng Wang, Zoubin Bi"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04443v1"
tags: ["query:wan-av-gen"]
score: 9.0
evidence: Wan-Streamer v0.2 是原生流式端到端音视频交互模型
tldr: 针对Wan-Streamer v0.1分辨率不足的问题，v0.2将交互输出流从192x336提升至640x368，同时保持模型侧约200毫秒低延迟。通过将thinker保留为单GPU低延迟流式感知路径，performer扩展为多GPU Ulysses上下文并行，高分辨率视频潜在序列分片去噪，音频序列不分片，实现场景细节可辨的实时对话。该架构升级在25FPS下维持紧凑的thinker-performer边界，总交互延迟约550毫秒，展示了高分辨率流式交互的可行性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04443v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1699, \"height\": 775, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04443v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 664, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04443v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 658, \"label\": \"Table\"}]"
motivation: 提升v0.1分辨率，使中景智能体的姿态、注视、手部等细节在实时对话中清晰可辨。
method: thinker单GPU处理流式感知与语言状态缓存；performer多GPU仅以K/V为条件，分片高分辨率视频潜在去噪，音频不分片。
result: 输出分辨率达640x368，模型侧延迟约200ms，总交互延迟约550ms（含350ms网络）。
conclusion: 实现了高分辨率、低延迟的实时音视频交互，验证了分离视觉扩展与语言通信的架构优势。
---

## 摘要
我们提出 Wan-Streamer v0.2，这是原生流式端到端视听交互模型的延迟保持升级版。v0.2 保留了 v0.1 的建模公式，但将交互式输出流的分辨率从 192×336 提升至 640×368，同时在 25 FPS 下保持约 200 毫秒的模型端信号到信号延迟。更高分辨率的流支持场景扎根的中景镜头智能体，其姿势、注视、手部、附近物体和局部场景布局在实时对话期间保持清晰可辨。为了支持更大的视觉流而不增加用户可感知的延迟，v0.2 将思考者保留为单 GPU 低延迟路径，用于流式感知、短语言/状态 Transformer 传递以构建生成缓存，以及最终解码。表演者成为一个多 GPU 尤利西斯式上下文并行组，用于昂贵的下一单元潜变量生成。每个表演者 rank 将传入的 K/V 写入预分片的本地缓存。长的高分辨率潜视频序列在 rank 间分片进行去噪，并通过尤利西斯通信收集，而短得多的音频潜序列则不分片生成。在这种拆分中，思考者的语言/状态计算结果仅作为 K/V 条件传递给表演者，因此在表演者组内无需通信单独的语言序列。这样将额外的硬件集中于视觉生成，同时保持了紧凑的思考者-表演者界限，当包括 350 毫秒双向网络预算时，总的远程交互延迟约为 550 毫秒。

## Abstract
We present Wan-Streamer v0.2, a latency-preserving upgrade of the native-streaming, end-to-end audio-visual interaction model. v0.2 keeps the v0.1 modeling formulation, but raises the interactive output stream from 192x336 to 640x368 while preserving approximately 200 ms model-side signal-to-signal latency at 25 FPS. The higher-resolution stream supports scene-grounded mid-shot agents whose posture, gaze, hands, nearby objects, and local scene layout remain legible during real-time conversation. To support the larger visual stream without adding user-visible delay, v0.2 keeps the thinker as a single-GPU low-latency path for streaming perception, the short language/state Transformer pass that builds the generation cache, and final decoding. The performer becomes a multi-GPU Ulysses-style context-parallel group for the expensive next-unit latent generation. Each performer rank writes incoming K/V into a pre-sharded local cache. The long high-resolution latent video sequence is split across ranks for denoising and gathered through Ulysses communication, while the much shorter audio latent sequence is generated without sequence sharding. In this split, the thinker's language/state computation reaches the performer only as K/V conditioning, so no separate language sequence has to be communicated inside the performer group. This concentrates additional hardware on visual generation while preserving the compact thinker-performer boundary, keeping total remote interaction latency at approximately 550 ms when a 350 ms bidirectional network budget is included.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

-   **核心问题**：实时音视频交互模型的视觉输出分辨率（v0.1版本的192×336）限制了其应用场景，尤其在中景或场景扎根的交互中，智能体的身体姿态、手势、周边物体及场景布局因过于压缩而难以辨认。
-   **整体含义**：论文提出了一个延迟保持的分辨率升级方案（Wan-Streamer v0.2），其核心意义在于证明，可以通过新型的服务拓扑设计，在不牺牲原版模型约200毫秒低延迟交互体验的前提下，将交互视频流的分辨率大幅提升至640×368。这使得实时对话智能体能够从“头像式视频通话”扩展至更广阔、更具沉浸感的场景化互动，为高清实时数字人、具身智能交互等应用提供了基础。

### 2. 论文提出的方法论

-   **核心思想：计算-通信解耦与专业化并行**
    模型沿用了v0.1的原生流式、端到端因果Transformer建模公式，但将服务的计算图在角色与物理边界上进行切分，以隔离新增的视觉生成成本：

    -   **思考者（Thinker）**：保留为单GPU，负责对延迟极其敏感的**控制路径**。任务包括流式音视频感知、语言/状态更新、为生成构建键值缓存，以及最终的因果解码。它是一个轻量化的交互核心。
    -   **表演者（Performer）**：扩展为一个多GPU的、尤利西斯风格的上下文并行组，专门负责计算耗时的**生成路径**，即下一单元的潜变量高分辨率视频生成。

-   **关键技术细节：紧凑的思考者-表演者边界与序列并行**
    -   **通信精简**：思考者仅向表演者广播紧凑的、表演者兼容的K/V切片作为生成条件。语言/状态序列本身并不需要在表演者组内通信，这极大地降低了通信开销，保持了紧凑的模块边界。
    -   **视觉生成并行化**：表演者组内的每个GPU维护一块预分片的本地K/V缓存。对于计算量大的高分辨率**视频潜变量序列**，将其在序列维度上分片至多GPU进行并行去噪处理，并通过尤利西斯全对全/收集通信进行状态同步。
    -   **音频生成策略**：鉴于音频潜变量序列极短，序列分片带来的通信开销将超过并行收益，因此采用不分片策略，直接生成。
    -   **工作流调度**：在每一个160毫秒的流式单元内，思考者完成感知并释放K/V切片，表演者并行接收并更新其分片缓存，同时独立进行下一单元的高分辨率视频潜变量去噪和音频潜变量生成。这一流水线设计确保了延迟不随分辨率增加而显著上升。

### 3. 实验设计

-   **评估基准（Benchmarks）**：
    -   **延迟指标**：核心对比点为v0.1版本。主要评估指标为**模型端到端信号延迟**（约200毫秒）以及在给定350毫秒双向网络预算下的**总远程交互延迟**（约550毫秒）。论文明确指出，其测量规范延续了v0.1的惯例，侧重于模型侧变化，外部网络波动是预设的外部假设。
    -   **定性评估**：未进行定量视觉质量测试，而是通过对生成的640×368分辨率的对话视频进行**定性视觉观察**，考察其稳定性与清晰度，具体关注面部细节、注视、口型、手部、姿态、近身物体和场景布局。

-   **对比方法**：
    -   **主要对比**：与v0.1版本进行直接对比，对比轴为输出分辨率、视觉格式与服务拓扑，同时强调延迟保持不变。
    -   **未进行的对比**：论文未将v0.2与其它外部实时交互模型（如文中“References”部分列举的系统）进行延迟或质量的量化对比，理由是这些系统报告延迟的端点定义各异，不具直接可比性。

### 4. 资源与算力

-   **算力与资源细节**：论文**未明确说明**模型训练所使用的GPU型号、数量、训练数据集大小或总训练时长。
-   **服务算力描述**：论文仅描述了**推理服务阶段**的硬件使用：思考者（Thinker）运行在单个GPU上，表演者（Performer）则运行在一个多GPU组上（具体GPU数量未说明）。性能数据（200毫秒模型侧延迟）即为在此未指明的推理硬件配置下测得。

### 5. 实验数量与充分性

-   **实验数量**：实验数量非常有限。主要由一个服务条件下的延迟运行性能测试和对生成结果的一组定性观察构成。
-   **实验充分性与客观性评估**：
    -   **不够充分**：严重缺乏定量实验佐证升级效果。例如，没有提供峰值信噪比、结构相似性指数、弗雷歇 inception 距离等常规视频质量量化指标，没有进行用户主观偏好研究，也没有消融实验来验证不同并行策略或分片粒度对延迟和资源利用率的具体影响。
    -   **客观性与公平性有局限**：仅与自身前代版本进行延迟对比，回避了与领域内其他先进实时音视频生成系统进行全面比较，这使得宣称的“高分辨率低延迟”方案在整个领域中的竞争力难以客观判断。结论的可信度高度依赖于论文发布的定性视频演示。

### 6. 论文的主要结论与发现

-   通过将延迟敏感的控制逻辑保留在单GPU思考者，并将高成本的视觉生成迁移到多GPU上下文并行的表演者，成功实现了在不牺牲原生流式模型原有低延迟（约200毫秒模型侧延迟）的前提下，将交互视频输出分辨率从192×336提升至640×368。
-   这种思考者-表演者分离的推理拓扑是有效的，它证明了可以将额外的硬件资源集中用于视觉质量改善，而保持轻量、低延迟的交互核心不受影响。
-   更高分辨率的输出使得生成的内容从简单的视频通话特写升级为可清晰展示身体语言和场景信息的“场景扎根式中景智能体”，拓展了模型的潜在视觉应用范围。

### 7. 优点

-   **架构创新性强**：提出了一种精巧且实用的异步服务拆分策略，将对延迟敏感的认知循环与对计算敏感的生成循环进行物理隔离和专业化加速，为解决实时大模型部署的难题提供了明确的工程范式。
-   **目标明确，设计聚焦**：直接挑战“高分辨率”与“低延迟”这一对核心矛盾，通过通信优化设计保持了模块间的紧凑边界，将额外计算开销锁定在特定模块内。
-   **工程价值高**：详细阐述了思考者与表演者间的K/V切片传递、预分片缓存、视觉序列并行等关键实现细节，对追求高性能实时多模态模型推理的工程化实践具有很强的参考意义。

### 8. 不足与局限

-   **实验验证严重不足**：几乎完全缺失定量结果，无法客观评估高分辨率视频的生成质量（保真度、时间一致性）以及升级后的画面相对于v0.1提升了多少。结论停留在定性和举例阶段。
-   **对比缺失**：仅与自己的v0.1版本做比较，未与行业内任何其他方法进行横向对比，无法确立其在该领域的技术先进性或独特性。
-   **算力与训练细节不透明**：全文未披露任何关于模型训练所用的算力规模、数据构成和训练时长的信息，这使得结果难以复现，也无法评估达到此性能的算力成本。
-   **应用分析局限**：仅从技术延迟层面讨论了“场景扎根”，但没有分析更高分辨率引入的挑战，例如生成内容的物理逻辑一致性（手物交互是否合理）、长视频稳定性、对复杂场景理解的准确性等是否也随之提升，可能在将技术优势泛化为应用能力时存在过度断言的风险。

（完）
