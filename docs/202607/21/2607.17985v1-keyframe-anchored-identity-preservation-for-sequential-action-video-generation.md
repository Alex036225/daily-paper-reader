---
title: Keyframe-Anchored Identity Preservation for Sequential-Action Video Generation
title_zh: 基于关键帧锚定的序列动作视频身份保持生成
authors: "Zhenjie Liu, Binyan Chen, Hao Chen, Tong Pan, Shangfei Wang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17985v1"
tags: ["query:wan-av-gen"]
score: 8.0
evidence: 基于身份保持关键帧的视频生成
tldr: 身份保持文本到视频生成中，指定时间顺序的动作序列易导致主体外观漂移。本文提出训练免费的三阶段框架：先进行动作感知提示润色，再通过关键帧锚定解耦外观与姿态，最后用多参考和噪声搜索增强身份一致性。方法在IPVG26挑战赛Track 2取得第三名，验证了无训练条件下连续动作视频生成中身份保持的可行性和竞争力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17985v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1815, \"height\": 875, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17985v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1832, \"height\": 608, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17985v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1821, \"height\": 914, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17985v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1819, \"height\": 235, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17985v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 530, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17985v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 879, \"height\": 222, \"label\": \"Table\"}]"
motivation: 端到端视频生成器在处理连续动作提示时，主体外观会随动作累积而漂移，难以保持身份一致性。
method: 构建训练免费的三阶段管道：动作提示润色、参考身份与前任帧联合的关键帧生成、多参考和噪声搜索的身份感知增强。
result: 在IPVG26官方Track 2排行榜上排名第三，证明方法有效且通用。
conclusion: 无需微调即可有效解决序列动作生成中的身份漂移问题，为时间结构化视频生成提供新思路。
---

## 摘要
身份保持的文本到视频生成旨在合成准确遵循文本描述并始终保持用户指定主体可识别性的视频。IPVG26挑战赛将此框架从单个整体提示扩展到时间结构化的规范。模型还接收带时间戳的动作描述序列，且必须按指定顺序渲染主体执行这些动作。这种时间结构带来了以往身份保持生成任务未遇到的挑战，因为主体必须在保持身份一致的同时持续执行脚本化的不同动作序列。然而，端到端视频生成器在运动累积和动作变化时容易出现外观漂移。我们通过一个免训练的三阶段流水线框架应对这一挑战。动作感知的提示润色阶段首先将输入重写为图像生成提示，明确每个动作的最终状态。然后，身份保持生成阶段通过联合调节参考身份及其前驱帧来生成关键帧序列，从而将时不变外观与时变姿态解耦。最后，身份感知的推理增强阶段使用多参考引导和身份驱动的噪声搜索来合成中间片段，两者在采样过程中强化身份保真度。我们的方法在官方Track 2排行榜上排名第三，展示了有竞争力的性能和强大的通用性。

## Abstract
Identity-preserving text-to-video generation aims to synthesize a video that accurately follows a textual description while maintaining the recognizability of a user-specified subject throughout. The IPVG26 challenge extends this framework from a single holistic prompt to a temporally structured specification. The model additionally receives a sequence of timestamped action captions and must render the subject performing these actions in the specified order. This temporal structure presents a challenge not encountered in previous identity-preserving generation tasks, as the subject must continuously perform a scripted sequence of distinct actions while maintaining a consistent identity. However, end-to-end video generators are prone to appearance drift as motion accumulates and the depicted actions change. We address this challenge with a training-free, three-stage pipeline framework. An action-aware prompt polishment stage first rewrites the inputs into image-generation prompts that specify the terminal state of each action. An identity-preserving generation stage then produces the keyframe sequence by conditioning each frame jointly on the reference identity and its predecessor, thereby decoupling time-invariant appearance from time-varying pose. Finally, an identity-aware inference enhancement stage synthesizes the intermediate segments using multi-reference guidance and identity-driven noise searching, both of which reinforce identity fidelity during sampling. Our method ranked third on the official Track 2 leaderboard, demonstrating competitive performance and strong generality.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，使用中文、以 Markdown 形式，对《Keyframe-Anchored Identity Preservation for Sequential-Action Video Generation》一文进行结构化、深入且客观的总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **研究背景**：文本到视频生成技术已取得巨大进步，但用户对内容的精确控制，尤其是对**主体身份保持（Identity Preservation）** 的需求日益增长。
*   **核心问题**：IPVG26挑战赛Track 2提出了一个更具挑战性的任务——**顺序动作视频生成**。模型需根据参考图像、全局场景描述以及一组带时间戳的动作描述，生成一段视频，其中主体必须按照脚本顺序执行不同动作，同时始终保持身份一致。
*   **现有方法与局限**：现有的端到端视频生成模型在处理此类任务时存在**外观漂移**问题。随着动作的累积和变化，主体的外貌会逐渐失真，无法在长序列中保持身份一致性。以往的微调方法（如LoRA）或参考条件方法（如自注意力注入）要么需要沉重的个性化成本，要么难以兼顾动作变化与身份保真度。
*   **整体含义**：本文旨在提出一种无需额外训练的通用方法，有效解决模型在生成连续、多样化动作时，主体身份随时间发生漂移的难题，从而提升用户在个性化内容创作中的控制力。

### 2. 论文提出的方法论

论文提出了一种免训练、三阶段的流水线框架，其核心思想是通过生成身份锚定的关键帧，将“时不变的主体外观”与“时变的姿态动作”解耦。

#### 2.1 核心思想：关键帧锚定分解
不直接一次性生成整个视频，而是将动作序列分解为一系列以动作终态为锚点的**身份保持关键帧**链，然后通过关键帧插值来合成视频片段。

#### 2.2 关键技术细节与流程

*   **第一阶段：动作感知提示润色**
    *   **目标**：将视频级别的叙事文本转化为空间精确、目标明确的图像生成提示。
    *   **流程**：使用多模态大语言模型，结合参考图像和时序描述，将每个动作段落重写为描述该动作**最终状态**的图像提示，明确主体的姿态、手势和视线方向。同时对全局场景描述进行润色，建立初始帧的场景布局。

*   **第二阶段：身份保持关键帧生成**
    *   **目标**：生成从初始状态到各动作最终状态的关键帧链，以解耦身份与姿态。
    *   **流程**：采用链式生成策略，使用一个图像生成模型分两步生成关键帧：
        1.  **生成首帧**：基于参考图像和润色后的全局提示生成初始姿态。
        2.  **生成后续帧**：第 `i` 个关键帧的生成同时接受三个信号：提供**时不变身份信息的参考图像**、提供**时变姿态/位置信息的第 `i-1` 帧**、以及描述**目标终态的第 `i` 个动作提示**。这使得模型仅修改提示词描述的动作，而保持身份不变。

*   **第三阶段：身份感知推理增强**
    *   **目标**：在由关键帧插值生成各动作片段时，进一步强化身份保真度。
    *   **流程**：
        1.  **可变长度片段分配**：根据每个动作的时间戳长度，按比例分配各片段的生成帧数。
        2.  **多参考引导**：
            *   **动机**：标准无分类器引导（CFG）不足以约束身份。
            *   **方法**：在每一步去噪中，将噪声预测分解为三个引导项的加权和：
                *   **文本引导**：增强与文本的语义对齐。
                *   **时空引导**：通过扰动注意力层来增强时序连贯性。
                *   **模态引导**：显式放大携带身份信息的视觉条件（边界关键帧）的影响。这通过计算条件预测与隐去视觉条件预测之差来实现。
        3.  **身份驱动噪声搜索**：
            *   **动机**：初始噪声对生成结果有显著影响。
            *   **方法**：对每个片段，采样 `K` 个候选噪声，对每个候选执行短步数（`T‘`）的“预览式”去噪，解码其第一帧，计算该帧与参考图像的人脸嵌入余弦相似度作为奖励，选择得分最高的候选噪声进行完整去噪。

### 3. 实验设计

*   **数据集与场景**：在IPVG26 Track 2的官方测试集上评估，该测试集包含**200个样本**，来自`ReactID`数据集。每个样本包含参考图、类别标签、全局提示和时序动作提示。
*   **Benchmark（评估指标）**：评估体系覆盖三个维度，共7个指标，最终综合成一个总分：
    *   **身份保持**：面部或物体特征相似度。
    *   **文本-视频对齐**：全局与分段的CLIP和GME相似度。
    *   **视觉质量**：运动平滑度（MS）和图像质量（IQ）。
    *   **最终排名分数**：加权求和，身份保持和时序对齐权重最高。
*   **对比方法**：
    *   **视频生成主干网络选择**：在统一的流水线框架下，对比了 **Wan2.1-VACE** 和 **LTX-2.3** 两种模型的性能。
    *   **最终榜单**：最终提交方案（USTC-AC）在官方排行榜上与其它参赛队伍（如USTC-CMI， WislabGDUT）进行排名对比。

### 4. 资源与算力

*   **文中未明确说明**：论文中未提及所使用的GPU型号、数量、整个流程的推理时间或任何与算力消耗相关的细节。
*   **推断**：由于方法是免训练的，所有组件均使用现成模型（Qwen3.6-27B， Z-Image, LTX-2.3）进行推理。算力消耗主要在模型的本地部署推理和噪声搜索的 `K` 次额外前向传播上，但具体资源需求未知。

### 5. 实验数量与充分性

*   **实验数量**：论文中呈现了约**3组主要实验**：
    1.  **主干网络定量对比**（表格1）：在200个测试样本上，系统对比了两种视频生成主干网络的全部7项指标，为一个重要的模型选择实验。
    2.  **官方榜单结果**（表格2）：展示了最终竞赛排名，验证了方法在外部、公平环境下的竞争力。
    3.  **推理增强消融实验**（表格3）：对两个核心推理增强组件（多参考引导和噪声搜索）进行了消融，分别探究其对整体和各分项指标的影响。
*   **充分性评估**：
    *   **优点**：实验设计逻辑清晰。消融实验直接针对论文的核心贡献点（增强策略），定量证明了各组件的有效性，特别是多参考引导的关键作用。
    *   **不足**：
        *   实验仅限于**单一竞赛数据集**（`ReactID`测试集），缺少在更通用或多样化的数据集上的验证。
        *   未与更多现有的免训练身份保持视频生成方法（若能适配此任务）进行直接对比，仅对比了内部管线使用不同主干网络的差异。
        *   **可靠性风险**：所有定量指标均由自动化算法评估，缺乏大规模用户主观评价，可能与人类感知存在偏差。

### 6. 论文的主要结论与发现

*   **方法有效性**：关键帧锚定分解是解决顺序动作视频生成中身份漂移问题的有效方法。
*   **性能验证**：基于此思想的免训练三阶段流水线在IPVG26挑战赛Track 2中取得了**第三名**的优异成绩，证明了其方法的竞争力。
*   **关键发现**：
    *   **LTX-2.3**作为视频生成主干，性能整体优于Wan2.1-VACE。
    *   在推理增强中，**多参考引导**是提升身份一致性的最主要驱动力。
    *   **身份驱动噪声搜索**作为一种补充性优化，能提供稳定但较小的增益。

### 7. 优点

*   **免训练架构**：整个流程无需任何模型微调或定制化训练，具有很强的通用性和即插即用性。
*   **优雅的解耦思想**：将复杂任务分解为“身份保持的关键帧生成”和“关键帧插值”，解耦了外观和动作两大挑战，构思巧妙。
*   **全面的身份约束**：在推理阶段综合运用了多参考引导和噪声搜索两种互补策略，从采样过程和初始化两个层面强化身份保真度。
*   **结果验证扎实**：在竞争性强的竞赛榜单上取得好名次，并通过消融实验清晰展示了各设计模块的贡献。

### 8. 不足与局限

*   **实验覆盖范围有限**：仅在竞赛的单一测试集上评估，缺乏在其他基准或开放域场景下的性能报告，泛化能力有待考证。
*   **计算成本不明**：未报告推理时间/显存消耗，尤其是噪声搜索带来的额外开销未知，难以评估其在实际应用中的效率。
*   **对基础模型的依赖性强**：流水线的性能高度依赖其集成的多模态大模型、图像生成模型和视频生成模型的单项能力，任何一个环节的短板都可能影响最终效果。
*   **评估的主观性风险**：仅依赖自动化指标，未进行人类偏好研究，可能在评估“身份感知”和“动作自然度”这类主观维度上出现偏差。
*   **复杂交互的挑战**：案例多为单人、简单动作，对于涉及多人、物体交互或大幅度遮挡的复杂动作序列，该方法可能面临挑战。

（完）
