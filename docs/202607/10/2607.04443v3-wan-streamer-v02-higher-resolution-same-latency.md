---
title: "Wan-Streamer v0.2: Higher Resolution, Same Latency"
title_zh: Wan-Streamer v0.2：更高分辨率，相同延迟
authors: "Lianghua Huang, Zhi-Fan Wu, Yupeng Shi, Wei Wang, Mengyang Feng, Junjie He, Chen-Wei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chen Liang, Cheng Yu, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Yuxiang Bao, Yuzheng Wang, Zoubin Bi"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04443v3"
tags: ["query:wan-av-gen"]
score: 9.0
evidence: Wan-Streamer v0.2是一个基于WAN的视听交互模型，用于多模态生成
tldr: 实时音视频交互中提升分辨率常导致延迟增加，Wan-Streamer v0.2通过优化系统架构解决此问题。它将thinker保持为单GPU流式感知，performer扩展为多GPU Ulysses并行生成，仅传递K/V条件以减少通信。输出分辨率从192×336升至640×368，模型端延迟仍约200ms，总交互延迟约550ms。该方案在不牺牲延迟下大幅增强视觉清晰度，支持实时场景中代理的姿势、视线等细节理解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04443v3/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1699, \"height\": 775}, {\"url\": \"assets/figures/arxiv/2607.04443v3/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 664}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04443v3/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 658}]"
motivation: 旨在提升实时交互视频分辨率，使半身代理的姿态、视线和场景细节清晰可见，同时维持极低延迟。
method: 保持thinker为单GPU低延迟路径，performer改为多GPU Ulysses上下文并行，长视频潜序列分片去噪，音频不分割，且仅传递K/V条件衔接。
result: 分辨率从192×336提升至640×368，模型端延迟保持约200ms，总交互延迟约550ms（含350ms网络预算）。
conclusion: 通过紧凑的thinker-performer边界与并行策略，在不增加延迟的前提下实现了高分辨率实时音视频交互，提升了人机对话的视觉理解质量。
---

## 摘要
我们提出Wan-Streamer v0.2，这是原生流式端到端视听交互模型的延迟保持升级版。v0.2保持了v0.1的建模公式，但将交互式输出流从192x336提升至640x368，同时在25 FPS下保持约200毫秒的模型端信号到信号延迟。更高分辨率的流支持场景中的中景代理，其实时对话期间的姿态、注视、手部、附近物体以及局部场景布局保持清晰可辨。为了在不增加用户可见延迟的前提下支持更大的视觉流，v0.2将思考者保留为用于流式感知的单GPU低延迟路径，以及用于构建生成缓存的短语言/状态Transformer通道和最终解码。执行者变为多GPU Ulysses式上下文并行组，用于昂贵的下一单元潜在生成。每个执行者rank将传入的K/V写入预分片的本地缓存。长高分辨率潜在视频序列被跨rank拆分进行去噪，并通过Ulysses通信收集，而短得多的音频潜在序列则无需序列分片生成。在这种划分下，思考者的语言/状态计算仅以K/V条件形式到达执行者，因此无需在执行者组内部通信单独的语言序列。这将额外的硬件集中在视觉生成上，同时保持了紧凑的思考者-执行者边界，在包含350毫秒双向网络预算时，总远程交互延迟约为550毫秒。

## Abstract
We present Wan-Streamer v0.2, a latency-preserving upgrade of the native-streaming, end-to-end audio-visual interaction model. v0.2 keeps the v0.1 modeling formulation, but raises the interactive output stream from 192x336 to 640x368 while preserving approximately 200 ms model-side signal-to-signal latency at 25 FPS. The higher-resolution stream supports scene-grounded mid-shot agents whose posture, gaze, hands, nearby objects, and local scene layout remain legible during real-time conversation. To support the larger visual stream without adding user-visible delay, v0.2 keeps the thinker as a single-GPU low-latency path for streaming perception, the short language/state Transformer pass that builds the generation cache, and final decoding. The performer becomes a multi-GPU Ulysses-style context-parallel group for the expensive next-unit latent generation. Each performer rank writes incoming K/V into a pre-sharded local cache. The long high-resolution latent video sequence is split across ranks for denoising and gathered through Ulysses communication, while the much shorter audio latent sequence is generated without sequence sharding. In this split, the thinker's language/state computation reaches the performer only as K/V conditioning, so no separate language sequence has to be communicated inside the performer group. This concentrates additional hardware on visual generation while preserving the compact thinker-performer boundary, keeping total remote interaction latency at approximately 550 ms when a 350 ms bidirectional network budget is included.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将基于提供的论文文本，为您生成一份结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义

-   **核心问题**：论文面临的核心挑战是，在将端到端实时音视频交互模型 **Wan-Streamer** 的输出视频流分辨率从 `192x336` 大幅提升至 `640x368` 时，如何**严格保持极低的模型端响应延迟（约 200 毫秒）不变**。更高的分辨率通常意味着更大的计算开销，容易导致交互延迟增加，破坏实时对话的自然感。
-   **研究动机与背景**：
    -   **视觉范围的局限性**： `Wan-Streamer v0.1` 的原生流式建模虽已实现了实时音视频交互闭环，但其原始 `192p` 的输出分辨率主要适用于面部特写视频通话，无法清晰呈现半身代理的身体姿态、手势、手持物体和周边场景等对场景化交互至关重要的视觉信息。
    -   **实时交互系统的通用难题**： 在全双工对话、流式视频生成和数字人系统等领域，普遍存在着提升生成质量（如分辨率）与保持低延迟之间的矛盾。许多系统为了高质量而牺牲了实时性，或采用了级联架构带来累积延迟。
-   **整体含义**： 本文并非提出全新的模型架构，而是一项**系统工程升级**。它表明，通过精心设计的推理服务拓扑（`Thinker-Performer` 解耦与并行策略），可以在不修改核心模型公式、不增加用户可感知延迟的前提下，成倍提升视觉输出的信息丰富度，从而将模型的交互能力从“高清通话”拓展到“场景化半身交流”。

### 2. 论文提出的方法论

核心思想是“**延迟保持型服务拓扑优化**”，即在保持 `Wan-Streamer v0.1` 因果流式建模公式不变的基础上，通过改变部署策略来吸收新增的生成算力成本。
-   **核心思想**：
    -   **Thinker-Performer 解耦与角色分化**：将推理过程明确分为延迟敏感的**控制路径**和计算密集的**生成路径**。
    -   **通信最小化**：仅传递紧凑的条件化信息（`K/V` 缓存切片）作为两个组件间的接口，避免了传递完整的、高维的语言序列。
-   **关键技术细节（服务架构）**：
    -   **Thinker（思考者）**：
        -   **角色**： 单 GPU，负责低延迟的交互循环。
        -   **功能**： 处理实时的用户音视频感知、语言/交互状态的 `Transformer` 更新、为生成过程构建 `K/V` 缓存，以及最终的因果解码输出。
        -   **定位**： 延迟关键路径。
    -   **Performer（执行者）**：
        -   **角色**： 多 GPU 并行组，负责高成本的视频潜空间生成。
        -   **功能**： 采用 **Ulysses 式上下文并行**，将长序列的高分辨率视频潜变量在多个 GPU `Rank` 上分片进行去噪，并通过 `all-to-all` 通信聚合结果。音频潜变量序列较短，则不分片。
        -   **缓存机制**： 每个 `Performer Rank` 维护预分片的本地 `K/V` 缓存，直接写入从 `Thinker` 接收到的 `K/V` 切片。
    -   **Thinker-Performer 交互边界**：
        -   `Thinker` 完成状态更新后，向 `Performer` 组广播兼容的 `K/V` 切片。
        -   `Performer` 组完成高分辨率视频潜变量的去噪生成后，将结果传递回 `Thinker` 进行解码输出。
        -   **关键点**： `Performer` 组内部无需通信语言序列，其所需的上下文完全由 `Thinker` 提供的 `K/V` 条件满足。

**算法与流程**
*系统中没有提出新的公式或模型架构，其“算法”体现在**推理调度流程**上：* 在每个 160 毫秒的流式单元 `k` 内，`Thinker` 消费当前用户观测并生成下一个 `K/V` 切片，同时接收并解码上一个单元的生成潜变量以发出响应；与此同时，`Performer` 接收当前的 `K/V` 切片，更新其历史缓存，并通过 Ulysses 并行方式为下一个单元执行视频潜变量的去噪生成。整个 `Performer` 组的工作加上所有通信开销必须在一个 160ms 的时间窗口内完成。

### 3. 实验设计

-   **数据集与场景**： 论文未提及任何用于**训练或定量评测**的特定数据集或 Benchmark。实验核心是**系统性能**和**定性视觉观察**。
-   **实验场景**： 从 `v0.1` 的“特写视频通话”扩展至 `v0.2` 的“高保真特写”和“场景化中景代理”对话。
-   **对比方法**： 本文的核心是对比版本之间的差异，即 **`Wan-Streamer v0.2` 相对于其基准 `Wan-Streamer v0.1`** 的系统能力升级。它没有与其他外部模型（如 GPT-4o、StreamAvatar 等）进行定量性能比较，仅在讨论延迟测量协议时提到了它们。
-   **评测指标**：
    -   **关键延迟指标**： 模型端信号到信号延迟（约 200 ms）、总远程交互延迟（约 550 ms，含假设的 350 ms 网络预算）。
    -   **关键能力指标**： 输出分辨率（`640x368`）、帧率（`25 FPS`）。
    -   **定性观察指标**： 视觉稳定性、清晰度（面部细节、口型、手部等）。
-   **测量协议**： 明确定义了延迟测量的边界：从 160ms 的用户流式单元对 `Thinker` 可用开始，到对应的音视频响应单元被解码为止。

### 4. 资源与算力

-   **硬件配置**： 论文没有指明具体的 GPU 型号。部署拓扑明确要求一个 `Thinker` 使用**单个 GPU**，而 `Performer` 使用**多 GPU** 组成的并行组。
-   **训练时长与算力**： **文中完全没有提及训练该模型所使用的 GPU 数量、训练时长或总计算量**。这是论文在可复现性方面的一个重要信息缺失。
-   **资源利用策略**： 论文的核心贡献在于如何通过 `Thinker` 和 `Performer` 的角色分工，将有限的硬件资源（多个 GPU）集中用于解决最昂贵的视觉生成瓶颈，同时保证控制链路的低延迟。

### 5. 实验数量与充分性

-   **实验数量**： 极为有限。论文本质上是一个**技术报告**，描述了系统升级与实现。其实验部分主要包含：
    1.  一组关键的**延迟基准测试**，用于证明延迟未增加。
    2.  一系列**定性视觉观察**来展示新分辨率下的输出效果。
-   **实验充分性与客观性**：
    -   **不充分之处**： 缺乏任何定量评估，例如：未报告高分辨率下的图像生成质量指标（如 `FID`、`IS`、`LPIPS`）、语音质量和音画同步的客观分数（如 `AV-Offset`、`SyncNet` 分数）、`Performer` 的吞吐量或并行效率。仅凭定性观察和一项延迟指标，无法客观、全面地衡量升级的效果。
    -   **公平性**： 作为内部的版本迭代升级报告，其对比是公平的，因为完全基于自身的 `v0.1` 基准。但要将其性能水平与其他外部系统对比，则缺乏数据支持。
    -   **实验设计客观性**： 预先设定了严格的延迟目标，然后仅证明其在实现更高分辨率的同时守住了这个目标，实验设计本身服务于这一明确目的，是直接的。

### 6. 论文的主要结论与发现

-   **成功实现零延迟代价的分辨率升级**： 通过创新的 `Thinker-Performer` 服务拓扑，`Wan-Streamer v0.2` 成功将输出流分辨率从 `192x336` 提升至 `640x368`，而**模型端信号到信号延迟精确地保持在约 200 毫秒**。
-   **架构解耦的有效性**： 论证了将延迟敏感的控制流与计算密集的视频生成流在硬件上分离，并通过紧凑的 `K/V` 缓存接口进行通信的策略，是实现实时高分辨率音视频交互的有效途径。
-   **交互范式的扩展**： 更高的分辨率使得模型的视觉交互能力从单纯的“口型面部”交流，扩展到能够理解代理的“身体姿势、视线、手和局部场景”的全新范式。

### 7. 优点

-   **方法创意与系统性**： `Thinker-Performer` 的分层式服务拓扑设计精巧，优雅地解决了提升算力需求与保持恒定低延迟之间的矛盾，是一个突出的系统工程创新点。
-   **清晰的界面定义**： 通过 `K/V` 缓存作为唯一通信桥梁，实现了思考者与执行者之间的干净解耦，具有高度的系统设计美感。
-   **目标导向明确**： 论文聚焦于一个非常具体、有挑战性的实际工程目标（更高分辨率，相同延迟），并最终通过架构设计达成，技术路线清晰有效。

### 8. 不足与局限

-   **实验评估匮乏**： 这是最主要的不足。完全没有定量评估生成质量，使得对“更高分辨率”的解读仅限于像素的增加，而其对细节清晰度、保真度的实际提升缺乏客观验证。
-   **缺乏与外部工作的比较**： 尽管在引言中列举了多个相关工作，但未与这些工作在任何指标上进行横向对比，难以判断其在整个实时数字人/多模态交互领域的技术水平。
-   **算力信息缺失**： 未提及训练资源消耗和详细的模型参数规模，影响了方法的可复现性和成本评估。
-   **网络延迟假设**： 总交互延迟中的 `350ms` 是一个外部网络预算假设，并非系统在真实网络环境下的实测值。这削弱了“总延迟约 550 毫秒”这一结论的现实意义。
-   **扩展性疑问**： 论文未讨论该方法在进一步提升分辨率（如 `1024p`）或帧率时，`Performer` 组的并行效率和通信开销是否会成为新的瓶颈。

（完）
