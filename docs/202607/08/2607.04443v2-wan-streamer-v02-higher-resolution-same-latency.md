---
title: "Wan-Streamer v0.2: Higher Resolution, Same Latency"
title_zh: Wan-Streamer v0.2：更高分辨率，相同延迟
authors: "Lianghua Huang, Zhi-Fan Wu, Yupeng Shi, Wei Wang, Mengyang Feng, Junjie He, Chen-Wei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chen Liang, Cheng Yu, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Yuxiang Bao, Yuzheng Wang, Zoubin Bi"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04443v2"
tags: ["query:wan-av-gen"]
score: 8.0
evidence: Wan-Streamer是一个端到端音视频交互模型，可生成流媒体视频
tldr: Wan-Streamer v0.2针对实时交互模型分辨率低、细节模糊的问题，通过分离thinker单GPU流式感知与performer多GPU Ulysses上下文并行生成，仅传递K/V缓存避免语言序列通信。在保持模型端约200ms延迟的前提下，将输出分辨率从192×336提升至640×368，总交互延迟约550ms。该升级使实时对话中智能体的姿势、凝视、手部及场景布局清晰可辨，支撑场景锚定的中景交互。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04443v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1699, \"height\": 775, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04443v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 664, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04443v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 658, \"label\": \"Table\"}]"
motivation: 提升实时视听交互的视觉细节清晰度，解决低分辨率下智能体姿势、手势与场景物体难以辨认的问题。
method: 保持thinker单GPU低延迟流式感知，将performer扩展为多GPU Ulysses上下文并行组，仅以K/V缓存传递语言/状态信息，视觉潜在序列分片去噪后通过Ulysses通信收集。
result: 输出分辨率提升至640×368，模型端信号-信号延迟仍维持约200ms，含350ms网络预算的总远程交互延迟约550ms。
conclusion: 通过紧凑的thinker-performer边界与并行策略，在不增加延迟的情况下大幅提升视觉分辨率，为场景细节丰富的实时智能体交互提供高效方案。
---

## 摘要
我们提出 Wan-Streamer v0.2，这是原生流式端到端音视频交互模型的保持延迟不变的升级版本。v0.2 保留了 v0.1 的建模公式，但将交互输出流的分辨率从 192×336 提升至 640×368，同时在 25 FPS 下保持约 200 毫秒的模型端信号到信号延迟。更高分辨率流支持场景扎根的中景智能体，其实时对话中的姿态、视线、手部、附近物体和局部场景布局保持清晰可辨。为了在不增加用户可察觉延迟的前提下支持更大的视觉流，v0.2 将思考者（thinker）保留为单 GPU 低延迟路径，用于流式感知、构建生成缓存的短语言/状态 Transformer 传递以及最终解码。执行者（performer）则成为一个多 GPU Ulysses 风格的上下文并行组，用于昂贵的下一单元潜在生成。每个执行者分片将传入的 K/V 写入预分片的本地缓存。长高分辨率潜在视频序列跨分片拆分以进行去噪，并通过 Ulysses 通信汇集，而短得多的音频潜在序列则无需序列分片生成。在此拆分中，思考者的语言/状态计算仅以 K/V 条件形式到达执行者，因此无需在执行者组内部通信单独的语言序列。这可将额外的硬件集中于视觉生成，同时保持紧凑的思考者-执行者边界，使得包含 350 毫秒双向网络预算时，总远程交互延迟保持在约 550 毫秒。

## Abstract
We present Wan-Streamer v0.2, a latency-preserving upgrade of the native-streaming, end-to-end audio-visual interaction model. v0.2 keeps the v0.1 modeling formulation, but raises the interactive output stream from 192x336 to 640x368 while preserving approximately 200 ms model-side signal-to-signal latency at 25 FPS. The higher-resolution stream supports scene-grounded mid-shot agents whose posture, gaze, hands, nearby objects, and local scene layout remain legible during real-time conversation. To support the larger visual stream without adding user-visible delay, v0.2 keeps the thinker as a single-GPU low-latency path for streaming perception, the short language/state Transformer pass that builds the generation cache, and final decoding. The performer becomes a multi-GPU Ulysses-style context-parallel group for the expensive next-unit latent generation. Each performer rank writes incoming K/V into a pre-sharded local cache. The long high-resolution latent video sequence is split across ranks for denoising and gathered through Ulysses communication, while the much shorter audio latent sequence is generated without sequence sharding. In this split, the thinker's language/state computation reaches the performer only as K/V conditioning, so no separate language sequence has to be communicated inside the performer group. This concentrates additional hardware on visual generation while preserving the compact thinker-performer boundary, keeping total remote interaction latency at approximately 550 ms when a 350 ms bidirectional network budget is included.

---

## 论文详细总结（自动生成）

好的，以下是基于您提供的论文内容，生成的结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **研究背景**：
    *   实时音视频交互处于全双工对话、多模态感知和流式视频生成的交叉领域，而 Wan-Streamer v0.1 为这一领域建立了原生流式模型框架，将用户与智能体的文本、音频和视频统一在一个因果时间线上。
    *   v0.1 的局限在于其输出分辨率仅为 192x336，仅能支持近景视频通话式交互，无法清晰呈现智能体的身体姿态、手部动作、周边物体和场景布局，限制了场景扎根的“中景”交互。

*   **核心问题**：
    *   如何在**不增加用户可感知延迟**的前提下，显著提升实时流式音视频交互模型的输出分辨率，以扩大视觉交互范围。
    *   更具体的技术挑战是：如何在仅约 200 毫秒的模型端响应延迟预算内，消化掉因分辨率从 192p 提升至 640x368 而急剧增加的计算开销。

*   **整体含义**：
    *   Wan-Streamer v0.2 并非旨在提出全新的模型架构，而是一项**保持核心算法不变、专注于系统与工程优化的升级**。它证明了通过巧妙的推理部署拓扑（思考者-执行者分离），可以在不牺牲实时交互体验（延迟）的前提下，大幅提升生成质量（分辨率），从而将实时数字人从“特写肖像”阶段推向了“场景化中景”阶段。

### 2. 论文提出的方法论

v0.2 的核心思想是通过**异构 GPU 拓扑**将低延迟的控制路径与高计算量的生成路径物理分离，以“不变应万变”。

*   **核心思想**：
    *   **异构分离**：将模型部署角色划分为“思考者”和“执行者”，两者承担不同性质的计算任务。
    *   **通信轻量化**：思考者至执行者的信息传递被浓缩为 Transformer 关键值缓存，避免了语言序列的冗余通信，保持了紧凑的边界。

*   **关键技术细节与算法流程**：
    *   **思考者** (单 GPU 低延迟路径):
        *   负责流式感知（音视频编码）、短语言/状态 Transformer 前向传播、构建 KV 缓存以及最终的解码（音频和视频解码器）。
        *   它是整个交互闭环的“神经中枢”，确保响应速度。
    *   **执行者** (多 GPU Ulysses 风格上下文并行组):
        *   承担昂贵的下一单元视频潜变量生成（流匹配去噪）任务。
        *   **预分片 KV 缓存**：每个执行者 GPU（分片）将接收自思考者的 KV 缓存片写入本地预分片缓存，避免了运行时开销大的全局缓存通信。
        *   **序列并行去噪**：长序列的高分辨率视频潜变量被**沿序列维度拆分**至所有执行者 GPU 上并行去噪。期间，通过 Ulysses 风格的全对全/收集通信来完成 Transformer 注意力计算。
        *   **音频处理**：由于音频潜变量序列较短，序列分片带来的开销将超过收益，因此音频潜变量**无需序列分片**生成。

*   **工作流时序** (如图 2 所示):
    1.  在流式单元 k，思考者消费用户输入，生成状态更新和可兼容执行者的 KV 缓存切片。
    2.  思考者广播 KV 切片给所有执行者分片，后者更新本地缓存。
    3.  执行者组在本地缓存条件下，通过 Ulysses 并行执行视频潜变量去噪，同时无分片地生成音频潜变量。
    4.  生成的音频视频潜变量返回思考者，由其解码并发送响应。
    5.  下一单元 k+1 开始，循环往复。整个过程（计算+通信）必须在一个 160 毫秒的单元内完成，以确保 200 毫秒的端到端信号延迟。

### 3. 实验设计

论文的实验设计相对简化，侧重于工程验证而非深度学术 Benchmarking。

*   **数据集/场景**：文中未提及使用了任何用于模型评估的标准数据集。定性观察仅基于**生成的 640x368 分辨率对话**。
*   **Benchmark/评估方式**：
    *   **延迟测量**：测量模型端“信号到信号”的延迟，起始于 160 毫秒用户流单元对思考者可用，结束于对应音视频响应单元解码完成。包括一个 350 毫秒双向网络的预算，用于计算约 550 毫秒的总远程交互延迟。
    *   **定性观察**：对生成的视频进行人工审查，关注点包括视觉稳定性、面部细节、视线、嘴部运动、手、姿势、附近物体和场景布局，特别是在聆听和说话的不同阶段。
*   **对比方法**：无。论文没有与任何其他模型（如 Hallo-live， StreamAvatar 或 GPT-4o 等）进行定量或定性对比。

### 4. 资源与算力

*   **GPU 型号与数量**：文中**未明确指出**所用 GPU 的具体型号。仅描述了拓扑结构：1 个 GPU 用于“思考者”，多个 GPU 组成“执行者”组，但未说明“执行者”组的具体 GPU 数量。
*   **训练时长**：**未提及**。论文聚焦于 v0.2 版本的推理/服务（Serving）部署，对模型本身的重训或微调过程及其所需算力只字未提。

### 5. 实验数量与充分性

*   **实验数量**：实验部分非常有限，仅包含 **1 组延迟指标报告** 和 **1 组定性视觉观察**。
*   **充分性与公平性**：
    *   **不充分**。实验严重缺乏用于衡量模型质量或交互效果的定量指标，例如视频质量、唇音同步精度、手势自然度或用户偏好。
    *   **不客观/难比较**。由于完全没有与任何基线方法（包括 v0.1）进行量化对比，读者无法客观判断分辨率提升对视频质量的增益程度，也无法判断其性能处于什么水平。
    *   **公平性（内部）**: 对延迟的评估通过明确测量边界、固定网络预算假设（350 毫秒）和维持与 v0.1 相同的测量约定，实现了内部一致性。

### 6. 论文的主要结论与发现

*   Wan-Streamer v0.2 成功地在保持约 **200 毫秒模型端延迟** 和约 **550 毫秒总交互延迟** 的前提下，将视频输出分辨率从 **192x336 提升至 640x368**。
*   这一升级是通过**创新的异构服务拓扑**实现的：一个单 GPU 思考器负责延迟关键路径，一个多 GPU Ulysses 并行执行器负责消化高分辨率带来的计算成本。
*   **通信优化**（仅传递 KV 缓存，视频潜变量序列并行）是维持紧凑思考者-执行者边界、避免通信瓶颈的关键。
*   v0.2 的视觉交互范围得以扩展，从特写镜头延伸到**场景扎根的中景智能体**，其身体、手势和场景细节在实时对话中清晰可辨。

### 7. 优点

*   **工程创新性强**：思考者-执行者分离的推理拓扑，以及利用 Ulysses 风格的上下文并行来分配视频生成负担，是一个优雅的系统级优化方案，有效解决了计算与延迟的矛盾。
*   **问题聚焦明确**：只专注于解决一个核心矛盾——“更高分辨率，相同延迟”，目标清晰，实现路径明确。
*   **通信设计高效**：将思考者到执行者的信息交互压缩为 KV 缓存切片，避免了昂贵且不必要的语言序列全量通信，设计精妙。
*   **应用价值高**：该工作直接赋能了下一代更逼真、更场景化的实时数字人应用，具有显著的工业落地潜力。

### 8. 不足与局限

*   **实验评估严重不足**：这是本文最显著的短板。研究工作仅报告了延迟指标和非正式的定性观察，缺乏视频质量、口型同步、手势自然度等任何量化评估，使得“更高分辨率”带来的实际视觉交互增益无法被客观衡量。
*   **缺乏对比实验**：完全没有与 v0.1 或其他同类系统（如 Hallo-live、StreamAvatar）进行横向对比，无法证明其方案的优越性。
*   **算力与资源信息模糊**：未公开 GPU 型号、数量或训练成本，降低了方案可复现性和后续参考价值。
*   **网络假设理想化**：总交互延迟的计算基于一个固定的、对称的 350 毫秒网络预算，这是一个较强且理想化的假设，实际网络波动会显著影响端到端体验。
*   **潜在过拟合或偏差风险**：由于仅展示了生成内容中的积极案例，无法评估模型在复杂光照、遮挡或长时序交互下视觉质量的稳定性，存在选择性呈现（Cherry-picking）的风险。

（完）
